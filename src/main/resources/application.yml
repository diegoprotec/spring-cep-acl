spring:
  profiles:
    default: prod

  application:
    name: cep-acl

server:
  port: 8081

aws:
  region: ${AWS_REGION:sa-east-1}

api:
  viacep:
    timeout-seconds: 5

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
