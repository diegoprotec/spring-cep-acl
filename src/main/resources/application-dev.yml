spring:
  config:
    activate:
      on-profile: dev

aws:
  endpoint: http://localhost:4566
  accessKeyId: dummy
  secretKey: dummy

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    operationsSorter: method
  show-actuator: true